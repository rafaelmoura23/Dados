# dataset = dados de futebol
# usando o view para mostrar os dados em formato de tabela-original
View(dataset)

# instalando o pacote do tydiverse para manipulação, exploração e visualização dos dados
install.packages("tydiverse")

# str para um overview dos dados/colunas
str(dataset) 

